
# Layout (desktop)
- Use a small Markdown table when comparing several rows (e.g. per-segment or before/after departure). Prefer these columns: `Segment | Operation | Timing | Penalty | Notes` and drop columns that would be empty.
- Use bullets or a short paragraph for single-value answers — do not force a table for one row.
